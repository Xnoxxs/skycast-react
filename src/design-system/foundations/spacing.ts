// Spacing scale — replaces magic numbers like padding: 16, margin: 24.
// Use these tokens everywhere instead of raw numeric values.

export const spacing = {
  xs: 4,  // tiny gaps, e.g. marginTop between label and value
  sm: 8,  // small internal padding
  md: 12, // default component gap
  lg: 16, // standard margin / padding
  xl: 24, // large section padding
}
