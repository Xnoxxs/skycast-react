// Centralized color tokens — every hardcoded color in the app lives here.
// Import from #design-system instead of writing hex values inline.

export const colors = {
  white: "#ffffff",
  black: "#000000",

  text: {
    primary: "#000000",   // default body text
    secondary: "#666666", // subtitles, helper text
    muted: "#888888",     // labels, de-emphasized info
    faint: "#444444",     // slightly muted, e.g. detail IDs
  },

  background: {
    screen: "#ffffff",
    card: "#ffffff",
  },

  shadow: "#000000",
}
